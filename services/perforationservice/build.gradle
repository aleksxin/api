
version '1.0'

mainClassName = "bg.keit.api.perforations.EurekaClientApplication"

jar {
    manifest {
        attributes 'Main-Class': 'bg.keit.api.perforations.EurekaClientApplication'
    }
}



dependencies {
    compile project(':datamodels:Perforations-model')
}